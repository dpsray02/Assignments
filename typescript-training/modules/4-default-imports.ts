import {subtract , PI} from '../modules/4-default-export.ts';
import abc from '../modules/4-default-export.ts';

console.log(`PI: ${PI}`);
console.log(`subtract(10, 4): ${subtract(10, 4)}`);
console.log(`add(5, 7): ${abc(5, 7)}`);

// for default import, we can use any name we want to import the default export from the module. In this case, we are using 'abc' as the name for the default import.
// we can use any name we want to import the default export from the module. In this case, we are using 'abc' as the name for the default import.