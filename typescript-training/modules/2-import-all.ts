import { PI, add } from '../modules/2-export-all.ts';

console.log(`PI: ${PI}`);
console.log(`add(2, 3): ${add(2, 3)}`);

//we need to import the exported variables and functions from the module using import statement.